#include <stdio.h>
#include "input.h"
#include "struct.h"

/*put the flag in the choosen box*/
void put_flag(int *n, int *p){
	
	printf("\nenter the line : ");
	scanf("%d", n);
	printf("\nenter the column : ");
        scanf("%d", p);

	return;
}
